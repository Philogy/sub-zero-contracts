
fn _CREATE<z0, create_succeeded>() -> (addr, word) {
    salt = calldataload(z0)
    word = 0x20
    cdz = calldatasize()
    size = sub(cdz, word)
    calldatacopy(z0, word, size)
    addr = create2(callvalue(), z0, size, salt)
    jumpi(create_succeeded, addr)
}

